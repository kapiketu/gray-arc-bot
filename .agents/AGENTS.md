# Custom Workspace Rules

- **DO NOT commit, upload, or register any new templates** to the system/codebase (including the `templates/` directory) without first generating a local preview link, presenting the link to the user, and obtaining their explicit approval to proceed.
